<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Input Field for the Love Message -->

    <!-- Language Selector Spinner -->
    <EditText
        android:id="@+id/messageEditText"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="20dp"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="start|top"
        android:inputType="textMultiLine"
        android:maxLines="10"
        android:overScrollMode="always"
        android:scrollbars="vertical"
        android:textColor="#FFFFFF"
        android:textSize="16sp" />

    <Spinner
        android:layout_marginTop="10dp"
        android:id="@+id/languageSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Voice Type Selector Spinner -->
    <Spinner
        android:layout_marginTop="10dp"
        android:id="@+id/voiceTypeSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Generate Button -->
    <Button
        android:id="@+id/generateButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:backgroundTint="#E91717"
        android:drawableLeft="@android:drawable/ic_lock_silent_mode_off"
        android:text="Generate Voice" />

    <!-- Share Button -->
    <Button
        android:id="@+id/shareButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableLeft="@android:drawable/ic_menu_share"
        android:text="Share Voice" />

    <!-- Download Button -->
    <Button
        android:id="@+id/downloadButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#0DEB16"
        android:drawableLeft="@android:drawable/stat_sys_download_done"
        android:text="Download Voice" />

</LinearLayout>